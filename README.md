# weather-data-storage-system
A simple implementation in Python that stores temperature records city-wise and year-wise using 2D arrays and an Abstract Data Type (ADT). Supports insertion, deletion, retrieval, row-major and column-major access, and sparse data handling.
